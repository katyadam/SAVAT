import { johnsonsAlgorithm } from "./cyclicDependency";
import { getMicroservicesBasedOnCouplingIndex } from "./highCoupling";

const Antipatterns = {
    johnsonsAlgorithm,
    getMicroservicesBasedOnCouplingIndex
};

export default Antipatterns;
