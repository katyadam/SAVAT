import React, { FC } from "react";

type ContextMenuType = {};

const ContextMenu: FC<ContextMenuType> = ({}) => {
  return <div className="bg-slate-600">Context Menu</div>;
};

export default ContextMenu;
